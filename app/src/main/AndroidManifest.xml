<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.city_bus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.City_bus">
        <activity android:name=".login.PasswordSuccessfullySetActivity"></activity>
        <activity android:name=".login.ResetPasswordActivity" />
        <activity android:name=".login.ForgetPasswordActivity" />
        <activity android:name=".login.SignupActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".startup.GetStarted3" />
        <activity android:name=".startup.GetStarted2" />
        <activity android:name=".startup.GetStarted1" />
        <activity android:name=".startup.Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>